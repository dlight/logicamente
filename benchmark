#!/bin/bash

time {
    for i in {1..1000}; do
	./limboole > /dev/null <<EOF
            ( ( o & ( ! ( ( ! ( ! ( ( v <-> ( ! ( ( ( ( q | ( ( ! ( ( ( ( v <-> (
            q & q ) ) | ( ! s ) ) | ( q <-> p ) ) <-> ( q <-> t ) ) ) | ( p | p ) ) )
            <-> ( p & s ) ) <-> ( s | o ) ) | ( q | q ) ) ) ) <-> ( s <-> v ) ) ) ) <->
            ( t | r ) ) ) ) <-> ( o & o ) )
EOF
    done
}